import { GeneralAnimationDirective } from './general-animation.directive';

describe('GeneralAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new GeneralAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
