import { ParallaxAnimationDirective } from './parallax-animation.directive';

describe('ParallaxAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
