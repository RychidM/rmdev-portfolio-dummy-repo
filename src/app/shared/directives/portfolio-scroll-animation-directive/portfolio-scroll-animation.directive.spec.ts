import { PortfolioScrollAnimationDirective } from './portfolio-scroll-animation.directive';

describe('PortfolioScrollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new PortfolioScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
