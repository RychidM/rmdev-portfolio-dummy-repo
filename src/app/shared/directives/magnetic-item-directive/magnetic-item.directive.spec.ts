import { MagneticItemDirective } from './magnetic-item.directive';

describe('MagneticItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MagneticItemDirective();
    expect(directive).toBeTruthy();
  });
});
