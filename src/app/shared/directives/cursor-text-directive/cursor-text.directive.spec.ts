import { CursorTextDirective } from './cursor-text.directive';

describe('CursorTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorTextDirective();
    expect(directive).toBeTruthy();
  });
});
