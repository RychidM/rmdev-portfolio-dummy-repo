import { ZoomInAnimationDirective } from './zoom-in-animation.directive';

describe('ZoomInAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
