import { HeroScrollAnimationDirective } from './hero-scroll-animation.directive';

describe('HeroScrollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroScrollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
